let User,jwt;_780‍.x([["register",()=>register],["login",()=>login]]);_780‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_780‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]); 

       const register = async(req, res) => {
    _780‍.g.console.log(req.body);
    const {name,email,password} =req.body;
    //validation
    if(!name) return res.status(400).send("Name is required");
    if(!password || password.length <6)
    return res
        .status(400)
        .send("password is required and should be min 6 character long");

    let userExist =await User.findOne({email}).exec()
    if(userExist) return res.status(400).send('Email is taken')
    
    //register
    const user =new User(req.body);
    try{
        await user.save();
        _780‍.g.console.log("USER CREATED",user);
        return res.json({ok:true});
    }catch (err){
        _780‍.g.console.log("CREATE USER FAILED",err);
        return res.status(400).send("Error,Try again");
    }

};

       const login= async(req,res)=>{
    //console.log(req.body);
    const {email,password}=req.body;
    try{
        //check if user with that email exist
        let user = await User.findOne({email}).exec();
        //console.log("user exists",user);
        if(!user) res.status(400).send("User with that email not found");
        //compare password
        user.comparePassword(password,(err,match)=>
        {
            _780‍.g.console.log("compare password in login err",err);
            if(!match || err) return res.status(400).send("wrong password");
            //Generate a token then send as response to client
            let token=jwt.token({_id:user._id},process.env.JWT_SECRET,{
            expiresIn:"7d",
            });
            res.json({token,user});
        });
    }catch(err)
    {
        _780‍.g.console.log("login error",err);
        res.status(400).send("signin Failed");
    }
};